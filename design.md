归一化：
目标

让各维变量处于同一尺度，保证：

RBF-SVR 的核距离合理；

全局探索距离项 
𝐷
(
⋅
)
D(⋅) 合理；

k-means、LHS、maximin 等空间填充性质不被量纲破坏。

做法（两层归一化，推荐）

物理空间边界缩放到 
[
0
,
1
]
𝑑
[0,1]
d
（用于 pool、距离、聚类、可视化）

𝑥
~
𝑗
=
𝑥
𝑗
−
𝑥
𝑗
,
min
⁡
𝑥
𝑗
,
max
⁡
−
𝑥
𝑗
,
min
⁡
x
~
j
	​

=
x
j,max
	​

−x
j,min
	​

x
j
	​

−x
j,min
	​

	​


SVR训练使用 z-score 标准化（用于提高SVR数值稳定性）

𝑧
𝑗
=
𝑥
𝑗
−
𝜇
𝑗
𝑠
𝑗
z
j
	​

=
s
j
	​

x
j
	​

−μ
j
	​

	​


𝜇
𝑗
,
𝑠
𝑗
μ
j
	​

,s
j
	​

 仅由当前训练集计算，并固定用于标准化候选池预测（避免泄漏）。

实现上：保存 normModel = struct('mu',mu,'sigma',s,'lb',lb,'ub',ub)。

pool：
目标

生成一个固定候选池（pool-based active learning），后续所有选点都从池中挑选。

同时保证：

概率意义（反映随机变量分布与截断范围）；

空间填充性（全局覆盖好，避免空洞）。

假设

变量相互独立（你已确认）。

生成方法：截断逆变换采样 + LHS（你提出的逻辑，正式写法）

对每一维随机变量 
𝑋
𝑗
X
j
	​

：

概率边界
给定物理截断区间 
[
𝑥
𝑗
,
min
⁡
,
𝑥
𝑗
,
max
⁡
]
[x
j,min
	​

,x
j,max
	​

]

𝑃
𝑗
,
min
⁡
=
𝐹
𝑗
(
𝑥
𝑗
,
min
⁡
)
,
𝑃
𝑗
,
max
⁡
=
𝐹
𝑗
(
𝑥
𝑗
,
max
⁡
)
P
j,min
	​

=F
j
	​

(x
j,min
	​

),P
j,max
	​

=F
j
	​

(x
j,max
	​

)

LHS 生成均匀分层样本
在 
[
0
,
1
]
[0,1] 上 LHS 得到 
𝑟
𝑖
𝑗
r
ij
	​

（i=1..Npool）

缩放到截断概率区间

𝑢
𝑖
𝑗
=
𝑃
𝑗
,
min
⁡
+
(
𝑃
𝑗
,
max
⁡
−
𝑃
𝑗
,
min
⁡
)
𝑟
𝑖
𝑗
u
ij
	​

=P
j,min
	​

+(P
j,max
	​

−P
j,min
	​

)r
ij
	​


逆映射回物理空间

𝑥
𝑖
𝑗
=
𝐹
𝑗
−
1
(
𝑢
𝑖
𝑗
)
x
ij
	​

=F
j
−1
	​

(u
ij
	​

)

最终得到 pool 矩阵 
𝑋
𝑝
𝑜
𝑜
𝑙
∈
𝑅
𝑁
𝑝
𝑜
𝑜
𝑙
×
𝑑
X
pool
∈R
N
pool
	​

×d
。

pool 的规模建议

二维测试：Npool = 1e4 ~ 5e4（画图更平滑）

d<10：Npool = 1e4 通常足够

pool 是 CPU 资源，不计入 FEM 次数。

pool 的分组（建议）

为了兼顾“全局优化器会跑到的区域”和“概率相关区域”，可在 pool 内标记两类来源：

pool_prob：按截断分布生成（上面流程）

pool_space：在物理边界上做 LHS/Sobol 生成后再截断/投影（用于更均匀覆盖边界）
合并：pool = [pool_prob; pool_space]，并打标签，便于分析与可视化。

初始DOE：
目标

在 pool-based 设置下，从候选池中挑 
𝑁
0
N
0
	​

 个初始点，既覆盖全局又不浪费 query。

推荐：k-means on pool（你已选择）

将 pool 先做 
[
0
,
1
]
𝑑
[0,1]
d
 归一化（上一节已有）。

在归一化空间对 pool 做 kmeans(k=N0) 得到簇中心 
𝑐
𝑘
c
k
	​

。

**把簇中心投影回 pool：**对每个中心选最近的 pool 点：

𝑥
𝑘
𝐷
𝑂
𝐸
=
arg
⁡
min
⁡
𝑥
∈
𝑝
𝑜
𝑜
𝑙
∥
𝑥
−
𝑐
𝑘
∥
x
k
DOE
	​

=arg
x∈pool
min
	​

∥x−c
k
	​

∥

去重：若多个中心投影到同一点，用次近邻替换。

初始样本数建议

𝑁
0
=
20
N
0
	​

=20（你预算 100 次时很稳）

若模型很平滑可取 15，但不建议更小。

SVR训练：
模型形式

回归：fitrsvm，核函数 RBF。

输出：
𝑔
^
(
𝑥
)
g
^
	​

(x) 预测 LSF 数值（不是分类）。

训练权重（LSF邻域优先）

对训练集中每个样本 
(
𝑥
𝑖
,
𝑔
𝑖
)
(x
i
	​

,g
i
	​

) 定义权重：

𝑤
𝑖
=
exp
⁡
(
−
∣
𝑔
𝑖
∣
𝜏
)
w
i
	​

=exp(−
τ
∣g
i
	​

∣
	​

)

𝜏
τ 推荐取当前训练集 
∣
𝑔
∣
∣g∣ 的 30% 分位数（动态更新）。

目的：显式提高 
∣
𝑔
∣
≈
0
∣g∣≈0 的拟合权重，符合你“全局LSF附近精度重要”。

不确定度估计：Bootstrap SVR 集成（用于U-function）

SVR本身没方差输出，采用 bootstrap 集成：

取 
𝑀
=
20
M=20 次 bootstrap 采样训练 M 个 SVR（使用同一组超参 
𝜃
θ，但训练子集不同）。

对候选点：

𝜇
(
𝑥
)
=
1
𝑀
∑
𝑚
=
1
𝑀
𝑔
^
(
𝑚
)
(
𝑥
)
,
𝜎
(
𝑥
)
=
1
𝑀
−
1
∑
𝑚
=
1
𝑀
(
𝑔
^
(
𝑚
)
(
𝑥
)
−
𝜇
(
𝑥
)
)
2
μ(x)=
M
1
	​

m=1
∑
M
	​

g
^
	​

(m)
(x),σ(x)=
M−1
1
	​

m=1
∑
M
	​

(
g
^
	​

(m)
(x)−μ(x))
2
	​


𝜇
,
𝜎
μ,σ 用于采样准则与在线纠偏触发。

A-mean（主模型预测）实现说明（已按此思路重构代码）

- 定义 ĝ(x) 为“单一主模型（main model）”在全训练集上训练得到的预测（加权 RBF-SVR；若无工具箱则用等价的 RBF-KRR）。
- bootstrap 集成训练得到的 M 个子模型仅用于估计不确定度 σ(x)（不再把集成均值 μ(x) 当作最终 ĝ(x)）。
- 采样准则（U-function）使用：U(x)=|ĝ(x)|/(σ(x)+ε)。
- Pf 估计默认使用：I(ĝ(x)≤0) 的样本比例。

最终模型保存形式

建议保存：

mainModel：主模型（用于 ĝ(x)）

sigmaModels{1..M}：bootstrap 子模型（用于 σ(x)）

theta：超参

normModel：归一化/标准化参数

trainData：X,g,w（用于复现实验）

调参策略：
目标

使用贝叶斯优化 + K折交叉验证（你指定），但避免每轮都调造成耗时和不稳定。

采用“前密后疏”的分段调参，并加入可选触发机制兜底。

超参集合

𝐶
C（BoxConstraint），
𝜖
ϵ，
𝛾
γ（或 KernelScale）

搜索空间用对数：

logC ∈ [log(1e-2), log(1e3)]

logEps ∈ [log(1e-4), log(1e-1)]

logGamma ∈ [log(1e-3), log(1e2)]（视归一化情况可微调）

调参实现路径（两档）

A. 快速工程版（你倾向）：

fitrsvm(...,'Weights',w,'OptimizeHyperparameters','auto', 'HyperparameterOptimizationOptions',...)

K折由 MATLAB 内部处理（可指定 KFold）。

B. 严格研究版（更贴合“加权CV目标”）：

自写 bayesopt 目标函数：K折 OOF 预测，计算加权RMSE/加权MAE作为目标

𝐽
(
𝜃
)
=
∑
𝑤
𝑖
(
𝑔
𝑖
−
𝑔
^
𝑖
)
2
∑
𝑤
𝑖
J(θ)=
∑w
i
	​

∑w
i
	​

(g
i
	​

−
g
^
	​

i
	​

)
2
	​

	​


这是最能保证“LSF附近优先”的方案。

建议：先用 A 跑通；二维解析例子做 A/B 对比。如果差异明显，再切换到 B。

分段调参间隔（你已接受“前密后疏”）

以样本数 N 为准：

𝑁
≤
30
N≤30：每次调参

30
<
𝑁
≤
50
30<N≤50：每2次调

50
<
𝑁
≤
75
50<N≤75：每5次调

𝑁
>
75
N>75：每10次调

实现：维护 lastTuneIter 和 gap(N)。

触发调参（可选兜底）

定义候选池整体不确定度：

Σ
𝑡
=
median
𝑥
∈
𝑝
𝑜
𝑜
𝑙
𝜎
𝑡
(
𝑥
)
Σ
t
	​

=median
x∈pool
	​

σ
t
	​

(x)

若：

Σ
𝑡
>
Σ
𝑡
−
1
(
1
+
0.05
)
Σ
t
	​

>Σ
t−1
	​

(1+0.05)

则触发一次调参。

关键约定（你问过）：

触发检测使用**上一轮超参 
𝜃
𝑡
−
1
θ
t−1
	​

**训练更新后的集成来计算 
Σ
𝑡
Σ
t
	​

；

触发后才运行 bayesopt 得到 
𝜃
𝑡
θ
t
	​

。

采样准则：
目标

平衡：

全局 LSF 附近精度（
∣
𝑔
∣
≈
0
∣g∣≈0 全局都要准）

全局探索覆盖

不确定度驱动（避免错判边界）

可靠度导向：SVR版 U-function

（A-mean 更新：用主模型 ĝ(x) 代替集成均值 μ(x)）

U(x) = |ĝ(x)| / (σ(x) + ε)
𝑈
(
𝑥
)
=
∣
𝜇
(
𝑥
)
∣
𝜎
(
𝑥
)
+
𝜖
𝜎
U(x)=
σ(x)+ϵ
σ
	​

∣μ(x)∣
	​


𝑈
U 小：接近边界且/或不确定度大 → 值得采样。

全局探索：最近邻距离项

在归一化空间：

𝐷
(
𝑥
)
=
min
⁡
𝑥
𝑖
∈
𝐷
∥
𝑥
~
−
𝑥
~
𝑖
∥
D(x)=
x
i
	​

∈D
min
	​

∥
x
~
−
x
~
i
	​

∥

并归一化到 
[
0
,
1
]
[0,1]。

融合 acquisition：带过渡的权重 αt

定义综合分数（越大越好）：

𝑆
(
𝑥
)
=
𝛼
𝑡
⋅
1
𝑈
(
𝑥
)
+
𝜖
+
(
1
−
𝛼
𝑡
)
⋅
𝐷
(
𝑥
)
S(x)=α
t
	​

⋅
U(x)+ϵ
1
	​

+(1−α
t
	​

)⋅D(x)
αt 如何取（可直接用）

推荐 logistic 过渡（稳定、可复现）：

𝛼
min
⁡
=
0.2
,
 
𝛼
max
⁡
=
0.9
α
min
	​

=0.2, α
max
	​

=0.9

总新增次数 
𝑇
=
𝑁
𝑚
𝑎
𝑥
−
𝑁
0
T=N
max
	​

−N
0
	​


𝑡
𝑐
=
0.35
𝑇
,
 
𝑘
=
10
/
𝑇
t
c
	​

=0.35T, k=10/T

𝛼
𝑡
=
𝛼
min
⁡
+
(
𝛼
max
⁡
−
𝛼
min
⁡
)
⋅
1
1
+
exp
⁡
(
−
𝑘
(
𝑡
−
𝑡
𝑐
)
)
α
t
	​

=α
min
	​

+(α
max
	​

−α
min
	​

)⋅
1+exp(−k(t−t
c
	​

))
1
	​


解释：

前1/3更偏探索（避免陷入局部边界细化导致全局薄弱）

后2/3更偏 LSF 精度（贴合RBDO最终会靠近LSF）

单点选择（你已决定不batch）

每轮：

𝑥
𝑛
𝑒
𝑤
=
arg
⁡
max
⁡
𝑥
∈
𝑝
𝑜
𝑜
𝑙
∖
𝐷
𝑆
(
𝑥
)
x
new
	​

=arg
x∈pool∖D
max
	​

S(x)

然后做一次 FEM query 得到 
𝑔
(
𝑥
𝑛
𝑒
𝑤
)
g(x
new
	​

)，加入训练集。

停止准则：

采用“双重模式”（你已认可并接受我的改进）：

1) 预算封顶

FEM 调用次数 ≥ 100 立即停止。

2) Pf 收敛（pool 上的预测失效概率变化小）

在固定的 MC 样本集合（可直接用 pool_prob 或另取固定 MC 集）上估计：

𝑃
^
𝑓
(
𝑡
)
=
1
𝑁
∑
𝑥
∈
𝑆
𝐼
(
𝜇
𝑡
(
𝑥
)
≤
0
)
P
^
f
(t)
	​

=
N
1
	​

x∈S
∑
	​

I(μ
t
	​

(x)≤0)

停止条件（相对变化）：

∣
𝑃
^
𝑓
(
𝑡
)
−
𝑃
^
𝑓
(
𝑡
−
1
)
∣
max
⁡
(
𝑃
^
𝑓
(
𝑡
−
1
)
,
10
−
6
)
<
𝜂
max(
P
^
f
(t−1)
	​

,10
−6
)
∣
P
^
f
(t)
	​

−
P
^
f
(t−1)
	​

∣
	​

<η

𝜂
η 推荐 1e-2 ~ 5e-3（二维测试可用更严格）

连续 K 次满足才停：K=3（防抖）

注：这里用 
𝜇
(
𝑥
)
μ(x) 判失效；若你想更保守，可用 
𝜇
(
𝑥
)
+
𝜅
𝜎
(
𝑥
)
≤
0
μ(x)+κσ(x)≤0 的形式做“安全侧”估计，但会影响 Pf 收敛速度。先用 
𝜇
μ 即可。

（A-mean 更新：这里的“判失效”应以主模型 ĝ(x) 为准；保守估计可用 ĝ(x)+κσ(x)≤0）

在线纠偏：
目标

离线训练保证全局 LSF 基础精度；

当未来 RBDO 进入收敛阶段，在设计点附近进一步加密，提高最终解附近边界精度；

总 FEM 次数仍≤100（离线+在线共享预算）。

两段式流程

离线阶段

用较宽松停止准则（Pf变化阈值可以稍大，或只用预算到某个比例停，比如先用掉 70~80 次）

在线阶段（轻量纠偏）
触发条件（你接受的 + 我补充的）：

设计点两次迭代距离小于阈值：

∥
𝑥
𝑑
(
𝑘
)
−
𝑥
𝑑
(
𝑘
−
1
)
∥
<
𝛿
𝑥
∥x
d
(k)
	​

−x
d
(k−1)
	​

∥<δ
x
	​


或者设计点邻域不确定度/边界判别不稳：

min
⁡
𝑥
∈
𝑁
(
𝑥
𝑑
)
𝑈
(
𝑥
)
<
𝑈
𝑡
𝑟
𝑖
𝑔
𝑔
𝑒
𝑟
x∈N(x
d
	​

)
min
	​

U(x)<U
trigger
	​


建议 
𝑈
𝑡
𝑟
𝑖
𝑔
𝑔
𝑒
𝑟
=
1.5
∼
2
U
trigger
	​

=1.5∼2

邻域加密选点

从 pool 中筛选邻域子池：

𝐶
𝑙
𝑜
𝑐
𝑎
𝑙
=
{
𝑥
∈
𝑝
𝑜
𝑜
𝑙
:
∥
𝑥
~
−
𝑥
~
𝑑
∥
<
𝑟
}
C
local
	​

={x∈pool:∥
x
~
−
x
~
d
	​

∥<r}

在子池上用同样 acquisition，但加大 
𝛼
α（强开发）：

在线阶段固定 
𝛼
=
0.9
∼
0.95
α=0.9∼0.95

半径 r 可逐步收缩（例如从 0.1 缩到 0.03 的归一化尺度）

为未来 RBDO 留接口（必须）

提供函数句柄/接口：

predictSurrogate(x) -> [ghat, sigma]（ghat 来自主模型，sigma 来自 bootstrap）

estimateReliability(design) -> Pf, beta（当前先占位）

onlineRefine(design, budgetRemain) -> updatedModel, usedBudget

二维解析测试输出：

你要求两种模式：仅离线；离线+在线。并输出/保存指定内容。

输入

2D 解析 LSF：
𝑔
(
𝑥
)
g(x) 可直接计算真值，用于评估/画图（算法本身仍按“隐式”流程写）。

变量边界、分布参数、截断区间。

总预算 100。

输出内容（必须）

估计失效概率收敛曲线

横轴：累计 query 次数

纵轴：
𝑃
^
𝑓
P
^
f
	​


离线与在线阶段分别标注（例如竖线分割）

最终总 query 次数

N_total, N_offline, N_online

二维空间采样点可视化

画真实边界 
𝑔
(
𝑥
)
=
0
g(x)=0（用网格求等值线）

标出：

初始 DOE 点（例如圆点）

主动学习新增点（例如叉号，按时间渐变/编号）

若有在线纠偏点，用不同符号/颜色标记

高维问题记录但不画图

保存所有采样点序列、每轮 Pf、Umin、alpha、超参θ历史等到 .mat

模型保存

model_offline.mat：离线训练完成的集成模型与归一化参数

model_online.mat：若触发在线纠偏，保存纠偏后模型；若未触发，可复制离线模型

建议的数据日志结构（MATLAB struct）

log.X_all, log.g_all, log.stage（0=DOE,1=offlineAL,2=onlineAL）

log.Pf_hat(t), log.Umin(t), log.alpha(t)

log.theta(t)（若某轮未调参可记录上一次）

log.timestamp（可选）

一句话版本的执行顺序（便于你写主脚本）

归一化参数与截断分布定义

生成 pool（截断逆变换 + LHS）

k-means 从 pool 选初始 DOE，FEM query 得到 g

训练加权SVR集成（bootstrap），按调参策略定期/触发更新超参

在 pool 上算 
𝜇
,
𝜎
μ,σ，用 
𝑆
(
𝑥
)
S(x) 选下一个点，循环到停止

若是“离线+在线”模式：接收设计点序列，满足触发条件则在邻域子池继续选点加密

输出二维图、Pf收敛、query次数，并保存模型与日志

如果你希望我继续把它变成可直接运行的 MATLAB 工程骨架（主函数 + 子函数列表），我可以按上面每一节对应提供函数接口与关键实现要点（例如 buildPoolTruncatedLHS.m, selectDOE_kmeans.m, trainBootstrapSVR.m, tuneHyperparams_autoOrBayesopt.m, acquisitionScore.m, stopCriteria.m, plot2DResults.m, saveModelAndLog.m），并保证离线/在线两模式开关清晰。
